export default function Home() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>SOP Writers Nepal</h1>
      <p>Messenger bot service — webhook running at <code>/api/webhook</code></p>
    </main>
  );
}
