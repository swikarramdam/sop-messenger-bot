export const metadata = {
  title: 'SOP Writers Nepal',
  description: 'Messenger bot service',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
